/**
 * CursorMode defines the interaction mode for the chromatic circle.
 *
 * - "info": Clicking a note displays information about that tone
 * - "select": Clicking a note toggles it in the selection set
 */
export type CursorMode = 'info' | 'select';
